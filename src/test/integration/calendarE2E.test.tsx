/**
 * INERT: Integration test disabled
 * This file has been turned into a placeholder to remove integration tests from the default test run.
 * To re-enable:
 *  - Restore the original test contents from git history or backup
 *  - Run `npm test` to verify the integration tests pass
 *  - If needed, reintroduce a temporary skip or a feature flag to toggle tests
 */
export {};
